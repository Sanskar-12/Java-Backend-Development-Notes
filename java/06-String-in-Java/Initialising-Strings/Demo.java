
public class Demo {
    public static void main(String[] args) {
        // One way of using String is below
        // String str=new String("Sanskar");

        // Or Other way of using it is below
        String str = "Sanskar";

        // Get the particular char of the string
        System.out.println(str.charAt(0));

        // Concatenate the words to the string
        System.out.println(str.concat(" Vishwakarma"));

        // Checks whether the ans contains in the String
        System.out.println(str.contains("ans"));

        // and there are many more methods in String
    }
}
